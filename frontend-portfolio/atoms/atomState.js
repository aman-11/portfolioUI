import { atom } from "recoil";

export const toolsAtom = atom({
  key: "toolsAtom",
  default: [],
});

export const skillsAtom = atom({
  key: "skillsAtom",
  default: [],
});

export const expAtom = atom({
  key: "expAtom",
  default: [],
});

export const workExpAtom = atom({
  key: "workExpAtom",
  default: [],
});
